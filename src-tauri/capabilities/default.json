{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The minimal Kestrel research window capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
